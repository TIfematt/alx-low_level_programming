#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 * main - Entry line
 * Return: Always 0 (success)
 */

int main(void)
{
	char low;

	for (low = 'a'; low <= 'z'; low++)
		putchar(low);
		putchar('\n');

		return (0);
}

