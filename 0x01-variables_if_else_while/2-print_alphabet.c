#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 * main - Entry line
 * Return: Always 0 (success)
 */

int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
		putchar(a);
	putchar('\n');

	return (0);
}

